# `unwrap_used` and `expect_used` are denied in `[lints.clippy]`. Tests are
# exempt: a panic there is a test failure, which is the intended outcome.
allow-unwrap-in-tests = true
allow-expect-in-tests = true
